{
  "Post": {
    "id": "Post",
    "required": ["id", "long", "lat", "message", "user_id", "tags", "relevance"],
    "properties": {
      "id": {
        "type": "string",
        "description": "Unique MongoDB ID of post"
      },
      "lat": {
        "type": "float",
        "description": "Latitude of the post",
        "minimum": "-90",
        "maximum": "90"
      },
      "long": {
        "type": "float",
        "description": "Longitude of the post",
        "minimum": "-180",
        "maximum": "180"
      },
      "message": {
        "type": "string",
        "description": "Text to display in the post"
      },
      "tags": {
        "type": "array",
        "description": "Tags this post was tagged with. Will be generated by the server and cannot be changed by the application."
      },
      "relevance": {
        "type": "integer",
        "description": "Relevance of this post. Can be used to compare it with other posts and decide which to display. Will be generated by the server and cannot be changed by the application."
      }
    }
  }
}